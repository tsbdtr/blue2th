[package]
name = "blue2th-frontend"
version.workspace = true
authors = ["tsbdtr"]
edition = "2021"
license.workspace = true
publish.workspace = true

[dependencies]
dioxus = { version = "0.7.10", features = ["mobile", "router"] }
rust-i18n = "3"
tokio = { version = "1", features = ["time", "rt"] }
blue2th-proto = { path = "../blue2th-proto" }
# No TLS backend: the backend is reached over plain HTTP on the LAN (a TLS option
# can be added later). `json` pulls in serde_json for response decoding; `stream`
# enables `bytes_stream()` for consuming the /scan SSE feed.
reqwest = { version = "0.12", default-features = false, features = ["json", "stream"] }
# The persisted app settings (phase 6.2) are serde types owned by the app, not
# shared DTOs, so the derive lives here rather than in `blue2th-proto`.
serde = { workspace = true }
serde_json = "1"
futures = "0.3"
# Phase 6.6: browsing `_blue2th._tcp.local.` on the LAN. See the workspace entry.
mdns-sd = { workspace = true }

[target.'cfg(target_os = "android")'.dependencies]
jni = { version = "0.22", default-features = false }
ndk-context = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

[features]
default = ["mobile"]
mobile = ["dioxus/mobile"]
